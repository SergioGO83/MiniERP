@{
    ViewData["Title"] = "Error";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-danger shadow-lg">
                <div class="card-header bg-danger text-white d-flex align-items-center">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i>
                    <h4 class="mb-0">Ha ocurrido un error</h4>
                </div>
                <div class="card-body">
                    <p class="lead text-muted">
                        Lo sentimos, algo no salió como esperábamos.
                        Nuestro equipo técnico ha sido notificado.
                    </p>
                    <hr />
                    <p class="text-secondary">
                        Puedes volver a la página principal y continuar con tu trabajo.
                    </p>
                    <a asp-controller="Dashboard" asp-action="Index" class="btn btn-primary">
                        <i class="bi bi-house-door-fill"></i> Volver al inicio
                    </a>
                    <a asp-controller="Account" asp-action="Login" class="btn btn-outline-secondary">
                        <i class="bi bi-box-arrow-in-right"></i> Iniciar sesión
                    </a>
                </div>
                <div class="card-footer text-muted text-end">
                    MiniERP © @DateTime.Now.Year
                </div>
            </div>
        </div>
    </div>
</div>
